import router from '@/router.js'

const state = {
  results: []
}

const getters = {
  results(state) {
    return state.results
  }
}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
